# Generated by XcodeGen — recreate with `xcodegen generate` (or ./build.sh)
Trident.xcodeproj/

# Build output
build/
.build/
DerivedData/

# Sparkle CLI tools (downloaded on demand) and release artifacts
.sparkle/
dist/

# Exported Sparkle private key — must never be committed
sparkle_private_key.pem

# macOS
.DS_Store
